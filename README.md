# Танковый бой

Браузерная многопользовательская игра про танки. Одна комната, вход по имени,
3D на three.js, авторитетный сервер на Node + WebSocket.

Сейчас реализовано: заезд в комнату, катание по карте с препятствиями,
столкновения, стрельба с уроном и респавном, ники и полоски здоровья над танками,
два режима — все против всех и волны ботов на четырёх уровнях сложности,
бонусные ящики на карте. Правила боя переключаются отдельно от режима: аркадные
с подписями над танками и реалистичные без них. Карта может быть с рельефом —
тогда у снаряда появляется высота, а у пушки вертикальная наводка. Два вида на
выбор — камера за танком и вид сверху для телефона; на бой это не влияет, и в
одной комнате они смешиваются свободно.

## Управление

| Действие | Клавиатура | Телефон |
| --- | --- | --- |
| Вперёд / назад | `W` / `S` | левая половина экрана — джойстик |
| Поворот корпуса | `A` / `D` (задним ходом руль работает наоборот — как у машины) | тот же джойстик |
| Башня и камера | мышь (клик по экрану — захват курсора) | правая половина экрана — свайп |
| Ствол по высоте (только на рельефе) | та же мышь: выше взгляд — выше ствол | ствол наводится сам |
| Огонь | ЛКМ или `Пробел` (удержание стреляет очередью по мере перезарядки) | кнопка «Огонь» |
| Приблизить / отдалить | колесо мыши | щипок двумя пальцами справа |
| Отпустить курсор | `Esc` | — |
| Сменить вид | `V` | галка «Вид сверху» в ⚙ |
| Настройки боя | `M` | кнопка ⚙ в углу |

## Два вида, одна комната

Вид — целиком дело клиента. Сервер шлёт всем одни и те же снапшоты, а во что их
превращать, каждый решает сам, поэтому телефон с видом сверху и ПК с камерой за
танком играют в одном бою, и в протоколе про это нет ни байта.

**Вид сверху** — ортографическая камера прямо над танком. Проекция именно
ортографическая, а не «перспектива с большой высоты»: у края экрана танк тогда
выглядит ровно так же, как в центре, и по картинке можно судить о расстояниях —
а на телефоне только по ней и судят.

| | Вид сзади | Вид сверху |
| --- | --- | --- |
| Наводка | камера, башня доворачивается за ней | напрямую: курсор от центра экрана или правый стик |
| Обзор | далеко вперёд, до 140 м | 10–54 м во все стороны, зум тот же |
| Стены | закрывают то, что за ними | видно поверх, но и тебя видно |
| Курсор | захватывается | свободен, он же прицел |

Размен честный: сверху видно за угол, но не видно на дистанции, с которой по
тебе работают боты (32–44 м) — до края обзора на среднем зуме 22 м.

Карта держится **севером вверх** и по курсу не разворачивается. Крутить весь
экран на каждом повороте гусениц — значит сделать картинку нечитаемой ровно там,
где она единственный источник сведений о мире: обзора вокруг себя, как в виде
сзади, тут нет.

Правый стик наводки на телефоне ещё и стреляет, если увести его дальше 72 %
радиуса. Иначе большой палец не успевал бы уходить с наводки на кнопку огня и
обратно, а каждая правка прицела означала бы выстрел не туда. Лёгкое касание
только доворачивает башню; кнопка «Огонь» при этом остаётся на месте.

Вся арифметика вида — в [topview.ts](src/client/topview.ts), отдельно от рендера
и без three. Ошибки здесь молчаливые: перепутанный знак в `up` или в `atan2` даёт
зеркальную карту, которая выглядит совершенно нормально, но танк едет не туда,
куда его ведут. Это и сверяет `npm run check:view` — оси экрана, обратный
пересчёт тычка в угол наводки и одинаковый обзор в портрете и в ландшафте.

## Карты

Девять планировок, переключает хост. Смена карты перезапускает мир: геометрия
уходит клиентам сообщением `map`, сцена собирается заново, все встают на спавны
новой карты — старые координаты на другой планировке могли бы оказаться внутри
блока.

| Карта | Что это | Чем играется |
| --- | --- | --- |
| Кремль | Открытое поле, блоки вокруг центра | Длинные дистанции, боты работают с любимых 32–44 м |
| Форт | Стены с четырьмя воротами, внутри двор | Есть что держать и куда отходить; ворота — точки боя и рикошетов |
| Город | Сетка кварталов, улицы по 12 м | Коротко и тесно, углы и рикошеты; ботам негде работать издали |
| Овраг | Стена делит карту надвое, три прохода | Фланги и засады у проходов; для волн честно, но однобоко |
| Окопы | Брустверы поперёк карты, проходы в разбежку | Стреляешь через всю карту, а едешь зигзагом и всё это время на виду |
| Автопарк | Ряды контейнеров, один ангар в центре | Парк простреливается поверху; за единственное укрытие дерутся |
| Дюны | Открыто, низкие барханы, три скалы | Самая снайперская: важна позиция, а спрятаться почти негде |
| Холмы | Рельеф вместо плоскости, блоков мало | Укрытие — сама земля; на 60 м гребни съедают треть выстрелов |
| Долина | 280×280, вдвое шире и выше «Холмов», хутор в центре и каменоломни по углам | Гребень стоит того, чтобы за него ехать: объехать — полминуты хода |
| Промзона | 280×280, почти плоская, кварталы цехов и дворов сеткой | «Вижу» и «достану» расходятся на каждом перекрёстке: цех держит снаряд, двор — только едущего |

«Долина» и «Промзона» вчетверо больше остальных (280×280 против 140×140) — это
задел под реализм: снаряд живёт 3.5 с и пролетает 217 м, и на такой карте
впервые пушка не добивает через неё насквозь. Их размер — свойство карты
(`MapDef.half`), а не общая константа: `stepTank`, `sweepShell`, `sweepTank`,
предсказание клиента и генератор рельефа принимают его параметром, и в аркадные
семь карт это не добавляет ни одной лишней проверки — они молча остаются при
исторических 140×140.

**Низкие укрытия.** Блок выше высоты полёта снаряда (2.15 м) укрывает от огня.
Блок ниже — только мешает ехать: через него видно, он простреливается насквозь,
а проехать нельзя. На этом и стоят три последние карты: «вижу» и «достану»
перестали быть одним и тем же, и позиция начала значить больше укрытия. В коде
это два списка вместо одного — `obstacles` (обо что можно удариться) и `cover`
(что держит снаряд). Второй считается один раз на смену карты: фильтровать его
в каждом свипе было бы самой дорогой строчкой сервера. По `obstacles` считаются
столкновения танков и щупы объезда у ботов, по `cover` — полёт снаряда, линия
огня, поиск рикошета и метка прицела на клиенте. Низкие блоки нарисованы другим
цветом: игрок должен видеть с одного взгляда, за чем можно спрятаться.

Правила для любой новой карты (в `src/shared/map.ts`): всё выровнено по осям,
иначе перестанут работать свипы и рикошеты; проезд не уже 8 м, иначе танк
радиусом 2.4 въедет и не выедет; высота блока — либо заметно выше 2.15 м, либо
заметно ниже, но не впритык, иначе снаряд будет то проходить, то нет; спавны
перечислены руками. Всё это проверяет `npm run check:map` — он же заливкой по
сетке ищет отрезанные карманы, до которых нельзя доехать.

Карты заметно отличаются по тому, насколько на них больно (`npm run bench:pve`,
входящий урон на «Новичке» и на «Асе»): «Автопарк» 2.1 и 11.1 ед/с, «Кремль»
1.7 и 13.0, «Дюны» 2.0 и 12.4, «Холмы» 2.0 и 12.2. На «Дюнах» укрытий почти нет
и стреляют издалека, на «Автопарке» контейнеры ломают подход ботам, но не мешают
им стрелять, на «Холмах» треть дальних выстрелов забирают гребни. На больших
картах входящий урон заметно ниже — «Долина» 0.8 и 4.2, «Промзона» 1.2 и 4.5, —
потому что боты те же самые, а сходиться им дольше: замер не про то, что бой
на них мягче, а про то, что дистанция впервые стала стоить времени.

Спавны ботов берутся из того же списка, что и людские: кольцо, которым они
раздавались раньше, на «Городе» прошло бы прямо сквозь кварталы.

## Рельеф

Карта может быть не плоской. Земля задаётся **высотным полем** — сеткой высот с
шагом 4 м и билинейной выборкой (`src/shared/terrain.ts`), а не произвольной
геометрией. Выборка стоит O(1), поле детерминировано, луч по нему трассируется
маршем со сравнением высот — тем же способом, каким свип ведёт снаряд, — а танк
остаётся кругом в плане: `resolveObstacles` рельеф не заметил вовсе, ему добавились
только высота и наклон.

**Поле приходит клиенту готовым массивом**, а не сидом генератора. Соблазн был:
одно число вместо тысячи. Но клиент предсказывает собственное движение той же
арифметикой, что и сервер, и любое расхождение генераторов дало бы тихий
рассинхрон — танк дёргался бы без всякой причины и без единой ошибки в логах.
Готовый массив ошибается громко или не ошибается вовсе. Высоты в нём целые, в
дециметрах: ровно те числа, по которым считают обе стороны, — сверять нечего.
Плоские карты поля не шлют вообще, поэтому семь аркадных не потяжелели ни на байт.

**Аркада — вырожденный случай.** Плоская карта отдаёт в свип `undefined` вместо
поля, и тогда он двумерный, ровно такой, каким был: третьего слэба у блоков нет,
марша по земле нет, вертикальной наводки нет — сервер держит её нулём независимо
от того, что прислал клиент. Это не «почти как раньше», а тот же самый код.

**Блоки стоят на площадках.** Прямоугольник, выровненный по осям, на склоне
повис бы одним углом, а укрытие превратилось бы в щель под стеной. Поэтому земля
под блоком выравнивается, а по краю площадка сходит на нет за пару клеток — и
«верх блока» снова просто `y + h`, как на плоскости.

**Вертикальная наводка появляется вместе с рельефом.** Пушка ходит от −9° до
+18°, как настоящая: ствол, смотрящий куда угодно, отменил бы рельеф. Со
склонением в девять градусов возвышенность даёт то, ради чего всё затевалось, —
сверху ты достаёшь того, кто снизу тебя уже не достаёт.

Ведёт наводку та же мышь, что двигает камеру по высоте: смотришь выше — ствол
выше. Луч камеры прицелом служить не может, камера здесь смотрит на свой же
танк, и её центр всегда упирается в землю рядом с ним. В виде сверху вести
наводку нечем — оба пальца заняты, — поэтому там ствол сам ложится на землю в
60 м по курсу. Метка прицела в обоих случаях считается настоящим свипом снаряда,
так что она честно садится на гребень перед тобой: «отсюда не достану» игрок
видит, а не выясняет выстрелом.

**Гравитации у снаряда нет и не будет.** При 62 м/с на двухстах метрах он падал
бы на полсотни: честная баллистика требует поднять скорость до реальных, а тогда
исчезает упреждение — то, на чём держится вся стрельба по движущимся и вся
разница между уровнями ботов. Тактику даёт не падение снаряда, а то, что у него
есть высота и рельеф её перекрывает.

Боты считают возвышение и линию огня тем же кодом: `hasShot` на карте с рельефом
проводит луч в трёх измерениях, и гребень отменяет выстрел так же честно, как
стена. Отсюда же вырастет засвет — луч у него будет тот же самый.

Размах холмов подбирается не на глаз, а по доле перекрытых линий огня: у «Холмов»
на 60 м рельеф съедает треть выстрелов, на 30 м — почти ничего. Дальний бой
становится вопросом позиции, ближний остаётся прежним. На «Долине» — том же
рельефе, но вчетверо большей карте (280×280) — холм растянут вдвое шире при том
же уклоне: гребень перестаёт быть складкой, которую переезжают мимоходом, и
становится направлением, которое выбирают на весь бой, а объехать его —
полминуты хода. Всё это сверяет `npm run check:terrain`: выборка и уклон, марш
луча по склону, площадки под блоками, попадание по высоте, дорога поля до
клиента без потерь, размер поля под размер своей карты — и отдельно то, что
семь аркадных карт остались плоскими 140×140, а их блоки стоят на нуле.

## Режимы

Комната одна, режим в ней переключает **хост** — первый вошедший игрок. Если он
вышел, хост переходит к следующему по времени входа. Смена режима перезапускает
мир: боты убираются, счёт обнуляется.

**Все против всех.** Бесконечная перестрелка, респавн через 4 с.

**Против ботов.** Волны. Играть можно одному или с кем угодно — команда общая,
против неё выходят боты. Волна `N` выпускает `4 + 2(N−1)` танков, но не всех разом:
на карте одновременно не больше `min(12, 4 + N, k·игроков)`, остальные подходят по
одному раз в 2.2 с. Волна закрыта, когда квота исчерпана и карта пуста — дальше
5 секунд передышки, и всё повторяется с большей волной.

**Толпа — это и есть сложность.** Множитель `k` берётся по тиру текущей волны:
двое на игрока на «Новичке» и «Среднем», трое на «Ветеране» и «Асе». Причина
арифметическая, а не вкусовая: перезарядка у всех одна, поэтому трое стреляют по
тебе ровно втрое чаще, чем ты по ним, и никакой точностью этот перевес не
отыгрывается. Считается `k` от тира волны, а не от выбора хоста, — иначе на
«Новичке» двадцать ботов поздней волны выходили бы по двое и волна превращалась в
тир. Квота волны от числа людей не зависит: одиночке достанется та же дюжина
танков десятой волны, просто меньшими порциями. Потолок гасит только пополнение:
если игрок вышел посреди волны, уже выехавшие боты с карты не исчезают — они
просто не заменяются.

**Бот тоньше игрока: 75 HP против 100.** Три попадания вместо четырёх. На равном
здоровье размен заведомо не в пользу человека — чтобы снять одного бота, нужно
четыре попадания и 6.4 с перезарядки, а стреляют в это время по тебе двое или
трое. Четверть времени волны возвращается игроку именно здесь.

**Жизнь одна на волну.** Подбитый ждёт до конца волны; если союзники дотянули её
до конца — он снова в строю. Новая волна начинается с полным HP у всех, включая
тех, кто дотянул предыдущую на последних очках. Полегли все — забег окончен, показывается достигнутая
волна и рекорд, через 8 с всё начинается с первой. Огонь по своим включён:
союзника снарядом задеть можно.

Ботов больше 12 не станет, поэтому после середины забега волна усиливается не
числом, а выучкой: каждые две волны тир поднимается на ступень, плюс в волне
появляется «элита» на ступень выше остальных. Выбранная сложность задаёт стартовый
тир, то есть насколько рано начнётся ад.

| Тир | На игрока | Наседают | Реакция | Ошибка прицела | Пауза сверх перезарядки | Упреждение | Держится не ближе | Рикошеты |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Новичок | 2 | 1 | 1.3 с | 0.26 рад | 2.6 с | нет | 46 м | нет |
| Средний | 2 | 1 | 0.6 с | 0.17 рад | 1.4 с | нет | 36 м | нет |
| Ветеран | 3 | 2 | 0.25 с | 0.085 рад | 0.6 с | 0.6 | 30 м | да |
| Ас | 3 | 3 | 0.11 с | 0.018 рад | нет | полное | 26 м | да |

Характеристики танка у ботов те же, что у людей: та же `stepTank`, та же
перезарядка, та же скорость доворота башни. Сложность меняет только качество игры —
иначе «Ас» ощущался бы как жульничество, а не как сильный противник.

**Реакция — это возраст картинки, а не пауза.** Бот наводится не туда, где цель
есть, а туда, где он её видел в последний раз; своё представление о ней он
освежает раз в «реакцию». Раньше это число только откладывало первый выстрел
после смены цели, а всё остальное время ствол держал цель идеально — и «Новичок»
вёл огонь с точностью, которой не бывает. Теперь по цели, идущей поперёк на
12 м/с, новичок целится в точку в 13.8 м от того места, куда снаряд придёт, а
ас — в 0.0 м (`npm run check:bots` печатает этот замер). По стоящей цели
отставания нет ни у кого: реакция наказывает за движение противника, а не за
существование, и стоять на месте от неё выгоднее не становится.

**Пауза сверх перезарядки** — единственный способ развести уровни по темпу огня.
Орудие готово, но новичок ещё возит стволом, прежде чем решиться. Без неё разница
между «Новичком» и «Асом» упиралась в одну точность, а точность против численного
перевеса не помогает: промахнувшись, толпа просто стреляет ещё раз.

**Замер ладдера.** Стенд `npm run bench:pve` сажает в комнату «героя» — человека,
которого водит тот же `think()` тира «Ас», то есть верхнюю планку того, как вообще
можно играть, — и гоняет забег до конца. Шесть забегов на клетку, «Кремль»:

| Сложность | Прожил на волне 1 | Входящий урон | Сбил ботов |
| --- | --- | --- | --- |
| Новичок | 35 с | 2.1 ед/с | 9.7 |
| Средний | 29 с | 3.6 ед/с | 3.8 |
| Ветеран | 12 с | 9.4 ед/с | 0.7 |
| Ас | 10 с | 10.4 ед/с | 0.7 |

До этой правки те же четыре строки укладывались в 8–15 с жизни и 7–12 ед/с
входящего урона: уровни отличались в полтора раза, волну 1 не проходил никто ни
на одной сложности, и «Новичок» ничем не отличался от «Аса». Разница была
арифметической, а не игровой — трое ботов с общей перезарядкой снимают 100 HP
быстрее, чем игрок успевает сделать шестнадцать выстрелов, нужных на волну.

**Упреждение — навык двух верхних тиров.** Снаряд летит 62 м/с, то есть с 45 м
идёт три четверти секунды, а танк за это время проезжает девять метров — вчетверо
больше собственного корпуса. Значит, без упреждения бот систематически бьёт в
след, и уклонение работает. Замер по цели, идущей поперёк на полном ходу (пустое
поле, один бот, 400 с на точку):

| Дистанция | Новичок | Средний | Ветеран | Ас |
| --- | --- | --- | --- | --- |
| 20 м | 38% | 40% | 48% | 47% |
| 45 м | 16% | 7% | 25% | 42% |
| 70 м | 8% | 6% | 17% | 37% |

Вблизи упреждение не решает — полсекунды полёта прощают всё. На дистанции ладдер
расходится: от «Среднего» на ходу уже можно уезжать, «Ас» попадает в двух случаях
из пяти. Побочный эффект видно в той же таблице: на 45 м «Новичок» попадает чаще
«Среднего». Оба бьют в след одинаково, но разброс «Новичка» вчетверо шире, и
случайный увод иногда сам гасит недолёт. Лечится либо сужением его разброса, либо
крошечным упреждением «Среднему».

**Слоты наседающих.** В ближний бой (до 10 м) идут только те боты, кому это
разрешает тир; остальные держатся на своей дистанции. Один наседающий — это дуэль,
в которой надо мансить, и это интересно. Трое разом на карте 140 м — это уже не
бой, увернуться нельзя никак. Поэтому на «Новичке» лезет один, на «Асе» — все три,
а на поздних волнах остальные девять работают издали.

**Манера боя — ручка, независимая от сложности.** Сложность отвечает за выучку
(реакция, точность, упреждение), манера — за дистанцию и за право лезть в упор.
Разводить их пришлось потому, что ощущаются они по-разному: «Ас» издали страшен
точностью, а «Новичок» в упор — тем, что от него некуда деться, и лечится это
тоже по-разному. Манера читается ИИ каждый тик, поэтому переключение действует
сразу, не дожидаясь новой волны.

| Манера | Что делает | Средняя дистанция до игрока |
| --- | --- | --- |
| Дистанция | множитель дистанций 1.45, наседать не может никто | 63 м |
| Нейтрал | тир как есть | 35 м |
| Напор | множитель 0.55, наседают все | 21 м |

Замер — `npm run check:bots`: человек стоит на месте, поэтому число зависит только
от манеры. Стоит знать, что «Напор» — это не «сложнее»: входящий урон у него
примерно как у нейтрала (3.1 против 2.9 ед/с на «Новичке»), и подъехавшего в упор
бота проще подбить. Меняется характер боя, а не его цена: разорвать дистанцию
не дадут.

Слот раздаётся без всякого сговора между ботами: каждый считает, сколько соседей
стоит между ним и целью, и лезет вперёд, только если их меньше, чем разрешено.
Правило чисто локальное, а строй из него получается общий.

**Вождение.** Объезд препятствий — веер щупов вокруг нужного курса: побеждает
направление, где до преграды дальше, со штрафом за отклонение. Трёх щупов не
хватало — на крутом обходе упирался и боковой, бот выбирал «менее плохую» сторону
и всё равно приезжал в угол. Газ дозируется по двум причинам: на полном ходу
корпус вращается вдвое медленнее, чем на месте (`TURN_RATE_FULL` против
`TURN_RATE_STILL`), поэтому танк с полным газом на довороте описывает дугу и
срезает угол блока; и подъезжать к стене на полной скорости незачем — лобовой
упор всё равно погасит ход. Если газ есть, а скорости нет полсекунды
подряд — бот признаёт упор и на 0.8 с сдаёт назад с вывернутым рулём; башня при
этом работает как работала. На замере (12 ботов, 80 с) доля времени в упоре
упала с 57% до 20%, средняя скорость выросла с 3.8 до 6.5 м/с.

**Когда настройки вступают в силу.** Карта, режим, правила, манера боя и бонусы —
сразу; карта, режим и правила перезапускают мир. Сложность — только на границе волн:
комната держит отдельно
выбор хоста и `runDifficulty`, по которой идёт текущая волна, иначе переключение
посреди боя меняло бы выучку следующих же ботов этой волны. Клиенту в `config`
приходят оба числа, поэтому панель может честно написать, что сейчас в бою и с
какой волны включится выбранное.

## Правила: аркада и реализм

Правила — **вторая ось, независимая от режима**. Режим отвечает за то, с кем
дерёшься, правила — за то, что игрок знает о мире, поэтому «все против всех» и
«против ботов» бывают и аркадными, и реалистичными. Переключает их хост, смена
перезапускает бой.

**Аркада** — то, чем игра была всегда: над каждым танком ник и полоска здоровья.
Кого поймал кадр, того и видно, и видно про него всё.

**Реализм** — подписей нет вовсе. Ни над противником, ни над собой. Противника
надо разглядеть, а не прочитать.

Своя подпись гаснет тоже, и это не оплошность: здоровье и так висит в HUD, а ник
с полоской над собственной башней — самое аркадное, что есть на экране, и висит
он в кадре постоянно.

Единственное исключение — **товарищ**. И тут важно, что «товарищ» ≠ «одна
команда»: в «Все против всех» номер команды у всех людей один и тот же, но
означает он только «за кого играешь», а огонь по своим включён. Поэтому союзником
там никто никому не приходится, и подписей в этом сочетании не остаётся ни одной.
Считает это `alliedTeams()` в [constants.ts](src/shared/constants.ts) — единственное
место, где правило записано, и его же гоняет `npm run check:bots`.

Решение о подписи принимает `plated()` в [main.ts](src/client/main.ts), а рендеру
достаётся готовый флаг через `setNameplate()`: сцена не знает ни про команды, ни
про режим комнаты, и знать не должна. Раздаётся флаг заново после смены правил
**и после смены режима** — во втором случае потому, что та же самая команда
союзником быть перестаёт.

Пока правила целиком клиентские: сервер шлёт всем одно и то же, подписи снимает
клиент. Настройка при этом комнатная, а не личная, — одни в комнате с ником над
головой, другие без, это не разные вкусы, а разные игры. Дальше в реалистичные
правила лягут засвет и рельеф, и вот они клиентскими уже не будут.

## Бонусы

Отдельный чекбокс в настройках хоста, работает в обоих режимах. На карте лежат
светящиеся ящики: подъехал — усилился. Появляются раз в 12 с, одновременно не
больше пяти, неподобранный исчезает через 45 с. Подбирают только люди — дюжина
ботов вымела бы карту раньше игрока.

| Ящик | Что даёт | Сколько держится |
| --- | --- | --- |
| Ремонт | +50 HP, но не выше максимума своего танка | мгновенно |
| Урон | снаряд снимает 40 вместо 25 | 20 с |
| Заряжание | перезарядка 0.8 с вместо 1.6 | 20 с |
| Ход | максимальная скорость и разгон +40% | 20 с |
| Маскировка | дальше 22 м танк не виден и не берётся ботами в цель | 15 с |

Второй ящик того же вида не складывается, а отсчитывает срок заново. Смерть
сжигает все набранные усиления — копить их через респавн нельзя.

«Ход» — единственный бонус, который меняет физику, поэтому его множитель ходит
через параметр `stepTank()`: сервер и предсказание клиента обязаны считать
движение одинаково, иначе свой же танк поедет мимо реконсиляции. Маска активных
эффектов приезжает в снапшоте полем `f`, и клиент подставляет из неё тот же
множитель. Расхождение возможно только на тике включения бонуса и гаснет само.

Маскировку сервер обеспечивает по-настоящему только против ботов: они перестают
брать замаскированного в цель. От живых игроков танк прячет клиент, то есть
на честность это полагается — для казуальной игры нормально, но если понадобится
строгость, фильтровать надо в снапшоте.

## Бой

100 HP, снаряд снимает 25 — четыре попадания на танк. Перезарядка 1.6 с,
после уничтожения танк возвращается через 4 с на свободной точке спавна.

Снаряд летит по прямой на 62 м/с, без гравитации: карту в поперечнике проходит
за 2.3 с, так что по едущей цели нужно упреждение. Летит он на высоте 2.15 м —
ниже любого блока на карте, поэтому за укрытием можно спрятаться.

**Выпущенный снаряд живёт сам по себе.** Стрелок за время полёта может погибнуть,
выйти из игры или потерять бонус на урон — на уже сделанный выстрел это не влияет.
Поэтому урон фиксируется в момент выстрела и хранится в самом снаряде (`ShellState.dmg`),
а не считается при попадании, а имя ушедшего стрелка комната держит в `ghosts`
ровно до тех пор, пока в воздухе есть хотя бы один его снаряд — иначе фраг от
погибшего бота уходил бы в ленту как «Неизвестный».

**Вдоль стены можно ехать.** Танк — круг, препятствия — прямоугольники, и
выталкивание из них двигает танк строго по нормали грани, продольную
составляющую хода не трогая. То есть скольжение вдоль грани геометрия давала и
раньше; останавливало танк не оно, а торможение — множитель, который снимал три
четверти хода за **любое** касание, хоть краем борта на полном ходу. Теперь сила
торможения считается по углу: проекция курса на нормаль грани. Ниже `BUMP_GRAZE`
(0.6, это около 37° к грани) стена не стоит ничего вовсе, дальше сопротивление
растёт квадратом, а лобовой упор гасит ход за 0.15 с.

Порог, а не плавная кривая, потому что мотор даёт всего `ACCEL`: любое торможение
сильнее него — это уже не «медленнее», а полная остановка. Без порога полоса
«едет, но вяло» получалась шириной градусов в пять. Замер (полный газ вдоль
одинокого блока, скорость через секунду, максимум 13 м/с):

| Угол к грани | 0° | 30° | 45° | 48° | 50° | 55° | 90° |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Скорость | 13.0 | 13.0 | 12.8 | 12.1 | 8.1 | 0 | 0 |

То есть угол можно срезать, вдоль стены — ехать, а въехать в неё под 60° и
поехать дальше — нельзя.

**Таран.** Столкновение танков со скоростью сближения выше 6 м/с наносит урон
обоим, до 30 на полном ходу. Делится он по вине: наехавший ест
`RAM_SELF_SHARE` (четверть) в любом случае, протараненный — полную цену, а в
лобовом сближении вина пополам и достаётся поровну. Наезд на стоящего на полном
ходу — это 30 ему и 8 себе, лоб в лоб — по 19 каждому. Урон округляется до целого:
физика считает его непрерывно от скорости сближения, а здоровье игрок читает с
полоски, и дробные остатки там ничего не решают. Контакт, округлившийся в ноль,
паузу между таранами не тратит — иначе им можно было бы прикрыться от настоящего. Убийство тараном идёт
в ленту и в счёт фрагов на общих основаниях: и выстрел, и таран проходят через
одну `hurt()`, потому что смерть тянет за собой фраг, ленту, сгорание бонусов,
выбывание бота и ожидание волны — второй экземпляр этого списка разошёлся бы с
первым на первой же правке.

Пауза между таранами — секунда, и она общая на танк, а не на пару: расталкивание
идёт каждый тик, и без паузы полусекундный контакт бортами означал бы пятнадцать
таранов подряд. Боты друг друга не таранят: они ходят стаей и постоянно трутся
бортами, обходя цель, — с уроном волна выкашивала бы себя сама, и чем больше
ботов, тем быстрее.

Метка прицела стоит не в центре экрана, а там, куда реально смотрит ствол: башня
доворачивается за камерой с задержкой и стреляет от дульного среза, так что центр
кадра — это не точка попадания. Луч метки считается тем же свипом, что и полёт
снаряда, поэтому она садится ровно на препятствие, в которое выстрел упрётся.

## Графика выстрела

Ничего из этого не стоит серверу ни байта: эффекты живут только на клиенте и
собираются из состояния, которое и так приходит в снапшоте.

| Что | Где крутить | Зачем |
| --- | --- | --- |
| Откат ствола | `RECOIL_BACK`, `RECOIL_RETURN` в `render.ts` | Ствол уходит назад рывком и выходит обратно экспонентой — видно, что выстрел был, даже когда снаряд уже улетел за кадр |
| Дульная вспышка | `MUZZLE_PRESET` | Конус вдоль ствола, 0.085 с. Короткая специально: длинный факел смазывается и читается как взрыв в упор |
| Дым от выстрела | `MUZZLE_SMOKE` | Всплывает и расплывается 0.7 с. Держим редким и небольшим — своя пушка стоит на линии взгляда |
| Трассер | `TRACER_LENGTH`, `tracerMaterial` | Хвост за снарядом, аддитивный и без записи глубины. По нему видно, откуда прилетело, и берётся упреждение |
| Толчок корпуса | `SHOT_KICK`, `HIT_KICK`, `KICK_DECAY` | Танк вздрагивает от своего выстрела и от прилетевшего снаряда — поверх ходового крена, со своим затуханием |
| Толчок камеры | `SHAKE_AMPLITUDE`, `SHAKE_DECAY`, `SHAKE_FREQ` | «Встряска» 0..1 затухает, сила берётся как её квадрат: мелкие толчки не мешают целиться, близкий фугас встряхивает по-настоящему |

Свой выстрел отыгрывается в момент нажатия, а не по снапшоту: перезарядка
считается теми же константами и на клиенте, и на сервере, так что расхождение
возможно только в спорный тик. Чужой — по первому кадру нового снаряда, но
вспышка ставится по матрице башни стрелявшего, а не по координатам снаряда:
тот к первому снапшоту успевает улететь на пару метров, и вспышка висела в воздухе.

Замаскированный танк выстрелом себя не выдаёт — иначе бонус переставал бы
работать ровно в тот момент, ради которого его берут.

Тряска от взрыва падает линейно до нуля на 26 м (`SHAKE_RANGE` в `main.ts`).
Отдельного «тряхнуть, когда прилетело» нет и не нужно: попадание в тебя — это
взрыв на нулевом расстоянии.

**Толчок корпуса: знаки здесь обратны ходовому крену.** Крен на повороте и клевок
на торможении растут от того, что упор земли **ниже** центра тяжести. Отдача же
приложена к башне, а прилетевший снаряд — к борту, то есть **выше**. Правило
выходит одно и простое: вверх идёт та сторона, с которой пришла сила. Стреляешь
вперёд — задирается нос, стреляешь на левый борт — поднимается левый, прилетело
в лоб — снова нос вверх. Это ровно тот сорт ошибки, который не видно на глаз:
наклон-то будет, просто не в ту сторону, — поэтому все восемь знаков проверяет
`check:effects`.

Кого качнуть при попадании, снапшот не сообщает — и не нужно. Снаряд взрывается
на границе круга цели, в 2.7 м от её центра, поэтому ближайший к взрыву танк и
есть тот самый; заодно из места взрыва берётся направление удара, которого в
сообщении не было бы вовсе. Взрыв гибели в этом не участвует: сервер ставит его
в центр танка, направления оттуда не вычесть, да и осанку остову задаёт сама
гибель.

## Ходовая часть: крен, следы, пыль

Корпус с башней сидит в отдельном узле внутри танка (`body`). Так и надо: сам
танк уже повёрнут по курсу, и наклон в его осях смешивал бы поворот с креном.

Крен и клевок считаются **из перемещения за кадр**, а не из ввода. У чужих
танков ввода нет вовсе, а свой рисуется предсказанием — одна формула на всех,
и боты кренятся ровно так же, как игрок. Скорость берётся как проекция шага на
курс, поэтому знаковая: на заднем ходу танк клюёт в другую сторону сам собой.

Знаки в `bodyLean()` неочевидны и разобраны там же в комментарии. Короче:
курс 0 смотрит в +Z, правый борт — это forward × up, то есть −X, значит растущий
курс поворачивает **влево**; в повороте корпус кренится наружу, вверх идёт левый
борт (+X), а это положительный поворот вокруг Z. С клевком наоборот: поворот
вокруг X кладёт нос вниз, а разгон должен нос задирать. Всё это проверяется
в `npm run check:effects` — руками такое не удержать.

Следы и пыль устроены одинаково и намеренно: кольцевой буфер фиксированного
размера, в который пишут **только в момент рождения** частицы. Затухание, рост
и полёт считает вершинный шейдер по возрасту, а возраст берёт из времени
рождения и одного общего uniform'а. Поэтому цена кадра не зависит от того,
сколько следов лежит на карте: процессор каждый кадр пишет ровно одно число.
Наивный вариант — пересчитывать прозрачность каждой частицы на процессоре —
на дюжине танков означал бы десятки тысяч записей и перезаливку всего буфера
в видеопамять каждый кадр.

| Что | Где крутить | Соображение |
| --- | --- | --- |
| Ёмкость следов | `TRACK_QUADS` = 2400 | Худший случай: 13 танков на полном ходу кладут ~390 отпечатков в секунду, на 6 секунд жизни нужно ~2400. Меньше — и след будет обрываться на полпути. 9600 вершин, индекс ещё влезает в 16 бит |
| Шаг отпечатка | `TRACK_STEP` = 0.85 м | Длина отпечатка (1.8 м) должна быть больше шага, иначе след выйдет пунктиром |
| Пыль | `DUST_STEP`, `DUST_MIN_SPEED` | Ниже 5 м/с пыли нет: на манёврах в упор она только мешала бы смотреть |
| Плотность пыли | множитель в фрагментном шейдере | За танком висит с десяток клубов внахлёст: на трети плотности это облако, на половине — стена |
| Размер клуба | аргумент `dust.emit` | В метрах, не в пикселях: шейдер сам пересчитывает в пиксели устройства через `uScale` |

Заливка в видеопамять — точечная (`addUpdateRange`): уезжает тронутая четвёрка
вершин, а не весь буфер. Ошибиться здесь не страшно на вид и дорого по факту —
картинка останется правильной, просто каждый кадр потащит сотню килобайт вместо
сотни байт. Единственное исключение — `clear()` при смене карты: он обязан
сбросить накопленные диапазоны, иначе рендерер зальёт только пару свежих
отпечатков, и следы прошлой карты остались бы лежать на новой.

Подбитый танк землю не месит, а замаскированный не выдаёт себя ни следом,
ни облаком пыли за спиной.

## Гибель танка

Подбитый танк темнеет до копоти, кренится, выбрасывает обломки и горит дымом
1.7 секунды — после чего оседает под землю и снимается со сцены.

**Почему остов не остаётся до возрождения.** На сервере подбитый танк выброшен
и из `resolveTankCollisions`, и из поиска цели снарядом: сквозь него ездят и
стреляют. В режиме волн павшие ждут конца волны, то есть остов простоял бы там
минуту — ложной мишенью, которая съедает по 1.6 с перезарядки за каждый выстрел
в неё. Поэтому гибель показываем и убираем.

**Почему остов тонет, а не растворяется.** Материалы корпуса общие на все танки
одного цвета, и гасить их прозрачностью значило бы гасить заодно живых. Земля
непрозрачна и прячет остов сама, бесплатно. Единственная плата — тени: земля
в карту теней не пишет, поэтому провалившийся танк продолжал бы бросать тень на
пустое место, и на время гибели `castShadow` с него снимается.

**Обломки — это тот же рой, что и пыль**, с другими числами: тяжелее, темнее,
с гравитацией и без разрастания. Разводить их в два класса значило бы дважды
написать кольцевой буфер и дважды — один и тот же шейдер.

**Бот горит так же, как игрок**, хотя сервер удаляет его из комнаты тем же
тиком, в котором тот погиб: волна кончается, когда карта опустеет, и держать
мёртвого в `players` значило бы держать волну незакрытой. Клиенту при этом
приходило одно сообщение `left` — то же самое, что и при выходе игрока, — и он
честно стирал танк, не успев узнать, что тот погиб. Боты просто исчезали с
карты. Теперь `left` несёт пометку `killed`, и сцена по ней оставляет остов
догореть, снимая его сама. Ставится он в очередь на те же 100 мс задержки
интерполяции, что и взрывы: сообщение о гибели приходит раньше, чем картинка
мира доедет до этого момента, и без очереди бот загорался бы до попадания.

Есть одна оговорка, без которой всё это ведёт себя странно: танк, которого
клиент **живым не застал**, хоронится молча. В режиме волн павшие ждут конца
волны, поэтому зашедший в середине волны получает их первым же снапшотом уже
подбитыми — без отметки `everSeen` на новичка разом посыпались бы чужие взрывы.

## Свечение, текстуры, танк

Три вещи, у которых общая черта: сервер о них не знает вообще. Всё это клиент.

### Свечение

`EffectComposer` с `UnrealBloomPass`, в полуплавающий буфер, с `OutputPass`
в конце — при рендере в буфер three пропускает тонмаппинг, и без последнего
прохода кадр вышел бы пересвеченным.

Главное здесь не проход, а порог. Он сравнивается с линейной яркостью кадра
**до** тонмаппинга, а не с тем, насколько ярким цвет выглядит: жёлтый снаряд
даёт 0.69, освещённая солнцем броня — 0.31. Подбирать порог между ними значило
бы балансировать в полосе 0.4, где любая правка света всё ломает. Поэтому порог
стоит на единице («ярче белого»), а светящееся поднято над ней множителем
`GLOW_*`. Все эти числа — свет, экспозиция, порог, множители, палитра — лежат
в [look.ts](src/client/look.ts): они связаны арифметикой, и разнесённые по
файлам позволяют правке освещения тихо утащить за порог половину карты.

Свечение — личная галка в панели, не сетевая: три прохода размытия по полному
кадру стоят заметно. Выбор помнится между заходами. Цепочка собрана всегда,
даже выключенная: собирать её на лету — перекомпиляция шейдеров рывком в бою.

### Текстуры

Генерируются кодом ([textures.ts](src/client/textures.ts)), файлов нет. Файл
текстуры — это артефакт, который надо взять с внятной лицензией, положить
в репозиторий, раздать через nginx и не забыть про кэш при выкладке.
Процедурный шум ничего из этого не требует и весит ноль байт в сборке.

Шум зациклен по решётке: земля повторяет текстуру сотню раз, и шов между
копиями лёг бы сеткой на всё поле. Все текстуры серые и светлые — они
умножаются на цвет материала, поэтому палитра та же, а умножение только
затемняет, то есть запас до порога свечения растёт, а не сокращается.

Развёртка коробок правится на геометрии (`scaleBoxUv`), а не отдельным
материалом на блок: у `BoxGeometry` все шесть граней размечены от нуля до
единицы независимо от того, сколько в них метров, поэтому одна текстура на
блоке 4×4 и на стене 144×2 выглядела бы то крупой, то пятном. Материал при
этом остаётся один на все блоки — сотня материалов означала бы сотню шейдеров.

### Танк

Наклонные листы, крылья, катки, ленивцы, маска пушки, дульный тормоз, кормовая
ниша, ящик ЗИП. Собирается из примитивов и **сливается в пять геометрий по
материалам** ([tank.ts](src/client/tank.ts)). Разница считается в вызовах
отрисовки: два десятка мешей на танк при дюжине танков — шестьсот вызовов за
кадр, да ещё столько же в проход теней. После слияния их пять на танк, ровно
как было у прежних коробок, а деталей на порядок больше.

Слить всё в одну нельзя, и границы не эстетические: ходовая красится в свой
тёмный цвет, металл — в свой, а ствол обязан ездить отдельно от башни, иначе
не сыграть откат.

Силуэт сохранён намеренно: по нему игрок оценивает дистанцию, а радиус
столкновений на сервере — 2.4 м и от внешнего вида не зависит вовсе. Стенд
проверяет, что танк не стал заметно шире, длиннее или уже прежнего.

**Модели GLTF здесь нет и не планируется без твоего решения.** Готовую модель
надо выбрать: лицензия, вес, количество полигонов, куда её класть и как
раздавать. Это не техническая задача, а выбор — поэтому геометрия пока своя.

## Запуск для разработки

```bash
npm install
npm run dev
```

`npm run dev` поднимает игровой сервер на `:8080` и vite на `:5173`.
Открывать нужно `http://localhost:5173` — vite сам проксирует `/ws` на сервер.
Чтобы проверить мультиплеер, открой вторую вкладку с другим именем.

Полезные команды:

```bash
npm run typecheck        # проверка типов
npm run check:map        # спавны не должны попадать внутрь препятствий
npm run check:smoothness # рывки предсказания в числах, сервер не нужен
npm run check:combat     # урон, перезарядка, укрытия, респавн — на живом классе Room
npm run check:bots       # волны, одна жизнь на волну, смена режима, бонусы, цена тика с 12 ботами
npm run check:effects    # знаки крена, геометрия следов, кольцевые буферы, уход остова — без браузера
npm run check:view       # оси вида сверху, наводка тычком, обзор в портрете и ландшафте
npm run check:terrain    # высотное поле, полёт над рельефом и неизменность аркады
npm run smoke            # два бота заходят, едут и стреляют; сервер должен быть запущен
npm run build       # клиент в dist/
npm run build:server # сервер в dist-server/
```

## Архитектура

```
src/shared/   логика, общая для клиента и сервера
  sim.ts        stepTank() — вся физика танка, один шаг
  constants.ts  тик, скорости, размеры карты
  map.ts        препятствия, рельеф и точки спавна
  terrain.ts    высотное поле: выборка, уклон, марш луча — без зависимостей
  protocol.ts   типы сообщений WebSocket
src/server/   авторитетный сервер: тик 30 Гц, снапшоты всем
  room.ts       мир, режимы и машина волн
  bot.ts        ИИ: бот — обычный игрок, которому вместо сети пишет think()
src/client/   three.js и интерфейс
  prediction.ts предсказание, реконсиляция, сглаживание — без DOM, поэтому тестируется
  topview.ts    оси вида сверху и наводка тычком — без three, поэтому тестируется
```

Ключевая идея: **движение описано ровно один раз** — в `stepTank()`.
Сервер крутит её как источник истины, клиент крутит ту же функцию,
чтобы предсказывать свой танк локально и не ждать ответа сервера.

Как это работает по шагам:

1. Клиент 30 раз в секунду шлёт **инпут** (`газ`, `руль`, `угол башни`, на рельефе
   ещё и возвышение ствола) с номером `seq`. Координаты клиент не присылает
   никогда — иначе телепорт-читы были бы бесплатны.
2. Сервер тикает 30 Гц, применяет инпуты и рассылает снапшот со своим `ack` —
   номером последнего применённого инпута каждого игрока.
3. Клиент, получив снапшот, ставит свой танк в серверную позицию и **переигрывает**
   все инпуты новее `ack`. Расхождение гасится плавно за пару кадров, без рывка.
4. Чужие танки рисуются с задержкой 100 мс и интерполяцией между снапшотами —
   так они едут плавно, а не телепортируются 30 раз в секунду.

Снаряды клиент не предсказывает: они рождаются и летят только на сервере, а
клиент рисует их из снапшотов той же интерполяцией и с той же задержкой, что и
чужие танки. Иначе снаряд и цель жили бы в разных моментах времени и попадания
не сходились бы с картинкой. Взрывы тоже придерживаются на 100 мс — вспышка
должна совпасть с тем кадром, где снаряд дошёл до места.

Компенсации лагов («попадание в прошлом») нет и пока не нужно: снаряд летит
2 с через карту, поэтому упреждение всё равно берёт игрок, а не сервер.

Два правила, которые легко нарушить и получить дрожащую картинку:

- **Кадр рисуется между шагами симуляции.** Шаг идёт 30 раз в секунду, экран —
  60–144, поэтому позиция всегда интерполируется по остатку аккумулятора.
- **Поправка сервера не двигает картинку.** Траектория сдвигается на поправку,
  а такой же противоход кладётся в `error` и гаснет за ~100 мс. Считать расхождение
  надо между чистыми состояниями симуляции: если взять нарисованную позицию,
  в него попадёт намеренное отставание кадра и сложится само с собой.

`npm run check:smoothness` меряет оба правила численно и сравнивает с реализацией
до исправления.

## Деплой (Ubuntu + Docker)

Два контейнера: `server` (игровой цикл) и `web` (nginx: статика + прокси `/ws`).

```bash
git clone <репозиторий> && cd tanks_battle
docker compose up -d --build
```

После этого игра доступна на `http://<домен>`. Проверить живость: `curl http://<домен>/health`.

Ресурсы поделены в `docker-compose.yml`: серверу 1 ядро и 1 ГБ, nginx — половина
ядра и 256 МБ. Игровой цикл однопоточный, так что второе ядро ему не нужно.

### Что должно быть на сервере

- **Docker Engine 20.10+ с плагином `compose` v2** (`docker compose`, не старый
  `docker-compose`). Больше ничего ставить не нужно: Node, npm и nginx живут
  внутри образов, на хосте их нет.
- **git** — воркфлоу поставит сам, если не найдёт.
- Пользователь деплоя должен быть в группе `docker` и запускать
  `docker compose` **без sudo**. Через `sudo docker compose` плагин compose
  часто не находится: он лежит в `~/.docker/cli-plugins` конкретного
  пользователя, а `sudo` подставляет `$HOME` от root.
  ```bash
  sudo usermod -aG docker <пользователь> && newgrp docker
  ```
- Свободные ~1.5 ГБ RAM на время сборки: образы собираются на самом сервере
  (`npm ci` + vite). На 4 ГБ VPS это проходит спокойно.
- ~2 ГБ диска под образы. Воркфлоу после деплоя делает `docker image prune -f`,
  так что старые слои не копятся.

### Порты

| Порт | Куда | Зачем |
| --- | --- | --- |
| 22 (или свой) | извне | SSH для деплоя |
| 80 | извне | игра и WebSocket `/ws` |
| 443 | извне | то же самое по TLS, см. ниже |
| 8080 | **только внутри** docker-сети | игровой сервер; наружу не публикуется |

В `docker-compose.yml` у сервиса `server` стоит `expose`, а не `ports`, — до
игрового сервера можно достучаться только через nginx. Открывать 8080 в фаерволе
не нужно и не следует.

### Автодеплой через GitHub Actions

Файл [.github/workflows/deploy.yml](.github/workflows/deploy.yml). Срабатывает на
push в `main` и по кнопке (`workflow_dispatch`). Два джоба:

1. **checks** — `typecheck`, `check:map`, `check:combat`, `check:smoothness`,
   `check:effects`, `check:view` и обе сборки. Сломанный коммит до сервера не доезжает.
   `check:bots` сюда не берём: он меряет время тика, а на общем раннере этот
   замер шатается — гейт деплоя не должен зависеть от загрузки чужой машины.
2. **deploy** — заходит по SSH, обновляет `/srv/tanks_battle` до `origin/main`,
   поднимает `docker compose up -d --build` и дожидается ответа от `/health`.

`down` перед `up` не делается: compose пересоздаёт только изменившиеся контейнеры,
поэтому простой — секунды, а не всё время сборки образа.

Секреты репозитория (Settings → Secrets and variables → Actions):

| Секрет | Что это |
| --- | --- |
| `SSH_HOST` | IP или домен VPS |
| `SSH_USER` | пользователь на VPS |
| `SSH_PORT` | порт SSH; можно не задавать, тогда 22 |
| `SSH_KEY` | приватный ключ, которым Actions заходит на VPS |

```bash
# Ключ раннера. Публичную половину — в ~/.ssh/authorized_keys на VPS,
# приватную целиком (со строками BEGIN/END) — в секрет SSH_KEY.
ssh-keygen -t ed25519 -f runner_key -N '' -C 'github-actions'
```

Доступ **сервера** к самому GitHub-репозиторию (`git clone`/`git fetch`) через
Actions не настраивается — это состояние сервера, а не репозитория. Ключ
генерируется прямо на VPS и остаётся там навсегда:

```bash
ssh-keygen -t ed25519 -f ~/.ssh/tanks-battle-deploy -N '' -C 'tanks_battle deploy'
cat >> ~/.ssh/config <<'EOF'
Host github.com
  IdentityFile ~/.ssh/tanks-battle-deploy
  IdentitiesOnly yes
EOF
```

Публичную половину (`tanks-battle-deploy.pub`) — в Settings → Deploy keys
репозитория на GitHub, доступа на чтение достаточно. Приватная часть с сервера
никогда никуда не уезжает, в секреты Actions её копировать не нужно.

Первый запуск сам склонирует репозиторий в `/srv/tanks_battle`, каталог заранее
готовить не нужно — только ключ и Deploy key должны быть на месте до первого
деплоя.

### HTTPS

Браузер разрешает `wss://` только с валидным сертификатом. Домен игры —
`tanksbattle.dotnetdon.ru`, `docker-compose.yml` и `docker/nginx-tls.conf` уже
на него настроены. Клиент сам выберет `ws://` или `wss://` по протоколу
страницы — менять код не нужно.

**Порядок важен**: `nginx-tls.conf` ссылается на файлы сертификата, которых
ещё нет. Если задеплоить этот конфиг раньше, чем сертификат появится на
сервере, `web` не запустится. Поэтому сертификат выпускается **до** пуша в
`main`:

```bash
sudo apt-get update && sudo apt-get install -y certbot
cd /srv/tanks_battle
docker compose stop web
sudo certbot certonly --standalone -d tanksbattle.dotnetdon.ru
docker compose start web
```

Дальше — обычный пуш в `main`: автодеплой поднимет `web` уже с TLS.

Правки `docker-compose.yml` и `nginx-tls.conf` нужно **коммитить в
репозиторий**, а не редактировать вручную на сервере: деплой делает
`git reset --hard origin/main` и стирает всё, что отличается от коммита.
Сертификаты в `/etc/letsencrypt` это не трогает — они не в репозитории.

Автопродление: пакет `certbot` сам ставит systemd-таймер, который дважды в
день проверяет срок годности. Но `standalone`-плагину на момент продления
нужен свободный порт 80, поэтому `web` нужно на секунду останавливать —
хуки делают это автоматически:

```bash
sudo mkdir -p /etc/letsencrypt/renewal-hooks/{pre,post}
printf '#!/bin/sh\ndocker compose -f /srv/tanks_battle/docker-compose.yml stop web\n' \
  | sudo tee /etc/letsencrypt/renewal-hooks/pre/stop-web.sh
printf '#!/bin/sh\ndocker compose -f /srv/tanks_battle/docker-compose.yml start web\n' \
  | sudo tee /etc/letsencrypt/renewal-hooks/post/start-web.sh
sudo chmod +x /etc/letsencrypt/renewal-hooks/pre/stop-web.sh /etc/letsencrypt/renewal-hooks/post/start-web.sh
```

## Что дальше

Реалистичный бой собирается по частям: подписи сняты, рельеф есть, большие
карты тоже. Дальше ложится вот что, и порядок здесь не случайный:

- [x] **Рельеф вместо плоскости** — высотное поле, высота у снаряда, вертикальная
      наводка. Устройство и решения — в разделе «Рельеф».
- [x] **Новые карты под реализм**, вчетверо больше нынешних (280×280): «Долина»
      и «Промзона». Побочное следствие приятное: снаряд живёт 3.5 с и пролетает
      217 м, то есть впервые пушка не добивает через карту. Старые семь остаются
      аркаде. Размер стал свойством карты (`MapDef.half`), а не общей константой.
- [ ] **Засвет в духе World of Tanks**: обзор, луч видимости, кусты (третья
      категория препятствий — не держат снаряд, но рвут луч), маскировка,
      удержание спота. Последним, потому что луч тогда пишется сразу по рельефу.
      Здесь же придётся разойтись с общим снапшотом на всех.
- [ ] Миникарта: без неё на карте 280×280 засвет некуда показывать.
- [ ] Возвышение чужого ствола в снапшоте: своё видно, чужое пока нет — на
      рельефе противник целится вверх и вниз, а его пушка на экране горизонтальна.

Аркада при этом остаётся вырожденным случаем всего перечисленного: нулевые
высоты, нулевой угол возвышения, выключенный фильтр видимости. Первые два уже
работают именно так.

Общее, не про правила:

- [ ] Таблица игроков со счётом (фраги уже считаются на сервере)
- [ ] Звук выстрелов и попаданий
- [ ] Разная броня по бортам
- [ ] Боты должны использовать укрытия осмысленно, а не только отходить на низком HP
- [ ] Переработать вид сверху
- [ ] Бинарный протокол вместо JSON, если станет тесно по трафику
